<Window x:Class="ChatApp.Client.Views.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Уведомление" Height="120" Width="350"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize">
    <Border Background="#007ACC" CornerRadius="10" BorderBrush="#005A9E" BorderThickness="2"
            Padding="15" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="315" ShadowDepth="5" Opacity="0.5" BlurRadius="10"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock x:Name="TitleText" Grid.Row="0" FontWeight="Bold" FontSize="14" 
                       Foreground="White" Margin="0,0,0,5"/>
            <TextBlock x:Name="MessageText" Grid.Row="1" FontSize="12" 
                       Foreground="White" TextWrapping="Wrap"/>
        </Grid>
    </Border>
</Window>
